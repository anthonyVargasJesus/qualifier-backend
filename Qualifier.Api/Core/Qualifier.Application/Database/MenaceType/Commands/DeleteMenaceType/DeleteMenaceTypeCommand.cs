using Microsoft.EntityFrameworkCore;
using Qualifier.Common.Application.Dto;
using Qualifier.Common.Application.NotificationPattern;
using Qualifier.Common.Application.Service;
using Qualifier.Domain.Interfaces;

namespace Qualifier.Application.Database.MenaceType.Commands.DeleteMenaceType
{
    public class DeleteMenaceTypeCommand : IDeleteMenaceTypeCommand
    {
        private readonly IDatabaseService _databaseService;
        private readonly IMenaceTypeRepository _menaceTypeRepository;

        public DeleteMenaceTypeCommand(IDatabaseService databaseService, IMenaceTypeRepository menaceTypeRepository)
        {
            _databaseService = databaseService;
            _menaceTypeRepository = menaceTypeRepository;
        }

        public async Task<Object> Execute(int id, int updateUserId)
        {
            try
            {
                Notification existsNotification = await this.existsValidationAsync(id);
                if (existsNotification.hasErrors())
                    return BaseApplication.getApplicationErrorResponse(existsNotification.errors);

                await _menaceTypeRepository.Delete(id, updateUserId);

                BaseResponseCommandDto baseResponseCommandDto = new BaseResponseCommandDto();
                baseResponseCommandDto.response = "¡Registro eliminado!";
                return baseResponseCommandDto;

            }
            catch (Exception)
            {
                return BaseApplication.getExceptionErrorResponse();
            }
        }

        private async Task<Notification> existsValidationAsync(int id)
        {
            Notification notification = new Notification();
            int total = await _databaseService.MenaceType.CountAsync(item => item.menaceTypeId == id);
            if (total == 0)
                notification.addError("El Id " + id.ToString() + " no se encuentra registrado");
            return notification;
        }

    }
}

