namespace Qualifier.Api.Helpers
{
    public static class CompanyConstants
    {
        public const int NO_COMPANY_ASSOCIATED = 0;
        // O si CompanyId no puede ser negativo, podrías usar un valor no numérico,
        // pero 0 es común para "no encontrado" en IDs enteros.
    }
}
